import { FC, ReactNode } from "react";

interface Props {
  children?: ReactNode;
}

export const {{ pascalCase name }}: FC<Props> = ({ children }) => (
  <div>
    <h1>{{ name }}</h1>
    {children}
  </div>
)
