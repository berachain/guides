{
  "name": "@berachain/governance-proposal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src/ --ext .ts,.tsx,.js --no-ignore --fix",
    "lint:fix": "eslint src/ --ext .ts,.tsx,.js --fix --no-ignore",
    "format": "prettier --write \"src/**/*.{ts,tsx,js}\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf node_modules; rm -rf .turbo;"
  },
  "devDependencies": {
    "@berachain/eslint-config": "workspace:*",
    "@berachain/typescript-config": "workspace:*",
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
