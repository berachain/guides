{
  "name": "@berachain/governance-proposal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node --check src/governance.js",
    "dev": "tsup --watch",
    "lint": "eslint src/ --ext .ts,.tsx,.js --no-ignore --fix",
    "lint:fix": "eslint src/ --ext .ts,.tsx,.js --fix --no-ignore",
    "format": "prettier --write \"src/**/*.{ts,tsx,js}\"",
    "test": "echo 'No test script defined for this guide'",
    "clean": "rm -rf node_modules; rm -rf .turbo;"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.0.0",
    "tsup": "^8.0.0",
    "typescript": "^6.0.0"
  }
}
