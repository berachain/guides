#!/usr/bin/env bash
# Validator-side config for install-helpers scripts.
# Copy to env.sh and set BEACOND_HOME.

BEACOND_HOME="${BEACOND_HOME:-}"

# Optional: beacond binary override
# BEACOND_BIN="${BEACOND_BIN:-beacond}"

# STAKING_POOL="0x..."   # optional; or pass --staking-pool to stake/unstake
# CHAIN="bepolia"
# DELEGATION_HANDLER="0x..."   # delegated pools only

NODE_API_ADDRESS="${NODE_API_ADDRESS:-127.0.0.1:3500}"
PRIVATE_KEY="${PRIVATE_KEY:-}"
