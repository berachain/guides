#!/usr/bin/env bash
# Staking Pool Configuration

# Path to beacond home (e.g., ~/.beacond or /path/to/berabox/installation)
BEACOND_HOME="${BEACOND_HOME:-}"

# Optional: Path to beacond binary (defaults to "beacond" in PATH)
# BEACOND_BIN="${BEACOND_BIN:-beacond}"

# Optional: Node API address for activation (leave blank to auto-detect from app.toml)
NODE_API_ADDRESS="${NODE_API_ADDRESS:-127.0.0.1:3500}"

# Transaction signing (optional - defaults to Ledger if not set)
# Option 1: Ledger hardware wallet (default if PRIVATE_KEY not set)
# Option 2: Private key from file (recommended - add ../private-keys.txt to .gitignore)
#    PRIVATE_KEY=$(sed -n '1p' "$(dirname "${BASH_SOURCE[0]}")/../private-keys.txt")
# Option 3: Direct private key (NOT recommended - never commit)
#    PRIVATE_KEY="0x..."
PRIVATE_KEY="${PRIVATE_KEY:-}"





